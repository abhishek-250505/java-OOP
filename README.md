# Java OOP Practice


CLASS AND OBJECT :-
- Class is like a blueprint.
- Object is an instance of class.
- We can store data using variables (like name, rollNo).
- We can create methods to perform actions.

I created a Student class.
In this class:
- name and rollNo are variables.
- study() method prints studying message.
- displayDetails() shows student information.

I created two objects (s1 and s2) and called their methods.

This helped me understand how objects work separately even though they belong to same class.

Still need to practice more to get fully comfortable.
